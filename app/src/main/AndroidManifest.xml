<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 权限声明 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32" /> <!-- 低于 Android 13 才需要 -->

    <!-- SDK 版本要求 -->
    <uses-sdk
        android:minSdkVersion="23" <!-- <-- 这里也改成 23 哦~ -->
        android:targetSdkVersion="34" /> <!-- 确保与 build.gradle 一致 -->

    <!-- 应用配置 -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight"
        tools:targetApi="34"> <!-- targetApi 建议与 targetSdkVersion 一致 -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SettingsActivity"/>
        <activity android:name=".AboutActivity"/>

        <!-- meta-data 放在 application 内部更规范 -->
        <meta-data
            android:name="VERSION_NAME"
            android:value="Beta1.0" />
        <meta-data
            android:name="VERSION_CODE"
            android:value="2554" /> <!-- 这个值应该与 build.gradle 中的 versionCode 一致 -->

    </application>

</manifest>
<!-- 确保 </manifest> 后面没有任何内容！ -->
