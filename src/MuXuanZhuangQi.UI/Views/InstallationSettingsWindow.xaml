<Window x:Class="MuXuanZhuangQi.UI.Views.InstallationSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="安装方式" Height="300" Width="300"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{materialDesign:MaterialDesignFont}">
    
    <StackPanel Margin="16">
        <RadioButton Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                    Content="系统安装"
                    IsChecked="{Binding InstallationType, 
                              Converter={StaticResource EnumToBooleanConverter}, 
                              ConverterParameter=System}"
                    Margin="0,0,0,8"/>
        
        <RadioButton Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                    Content="Shizuku"
                    IsChecked="{Binding InstallationType,
                              Converter={StaticResource EnumToBooleanConverter},
                              ConverterParameter=Shizuku}"
                    Margin="0,0,0,8"/>
        
        <RadioButton Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                    Content="Root"
                    IsChecked="{Binding InstallationType,
                              Converter={StaticResource EnumToBooleanConverter},
                              ConverterParameter=Root}"
                    Margin="0,0,0,8"/>
    </StackPanel>
</Window>