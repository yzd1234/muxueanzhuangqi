<Window x:Class="MuXuanZhuangQi.UI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="慕雪安装器 - 设置" Height="400" Width="300"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{materialDesign:MaterialDesignFont}">
    
    <StackPanel Margin="16">
        <!-- 安装方式 -->
        <Button Style="{StaticResource MaterialDesignFlatButton}"
                Command="{Binding OpenInstallationSettingsCommand}"
                HorizontalContentAlignment="Left"
                Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Download" Margin="0,0,8,0"/>
                <TextBlock Text="安装方式"/>
            </StackPanel>
        </Button>

        <!-- 主题设置 -->
        <TextBlock Text="主题"
                  Style="{StaticResource MaterialDesignBody1TextBlock}"
                  Margin="0,16,0,8"/>
        <RadioButton Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                    Content="亮色"
                    IsChecked="{Binding Theme, 
                              Converter={StaticResource EnumToBooleanConverter}, 
                              ConverterParameter=Light}"
                    Margin="0,0,0,8"/>
        <RadioButton Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                    Content="深色"
                    IsChecked="{Binding Theme,
                              Converter={StaticResource EnumToBooleanConverter},
                              ConverterParameter=Dark}"
                    Margin="0,0,0,8"/>
        <RadioButton Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                    Content="跟随系统"
                    IsChecked="{Binding Theme,
                              Converter={StaticResource EnumToBooleanConverter},
                              ConverterParameter=System}"
                    Margin="0,0,0,8"/>

        <!-- 关于 -->
        <Button Style="{StaticResource MaterialDesignFlatButton}"
                Command="{Binding OpenAboutCommand}"
                HorizontalContentAlignment="Left"
                Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Information" Margin="0,0,8,0"/>
                <TextBlock Text="关于"/>
            </StackPanel>
        </Button>
    </StackPanel>
</Window>